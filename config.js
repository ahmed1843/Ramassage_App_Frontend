// config.js
const CONFIG = {
    API_URL: "http://localhost:3000", // L'adresse de ton futur backend
    APP_NAME: "New Déchets",
    VERSION: "1.0.0"
};

// On rend CONFIG accessible partout
window.CONFIG = CONFIG;
